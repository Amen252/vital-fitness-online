# Vital Fitness — Mobile

Flutter app for User and Coach. Package name remains `vital_fitness`.

```bash
cd mobile
flutter pub get
flutter run
```

API default: `http://127.0.0.1:5050/api` (see `lib/config/api_config.dart`).
